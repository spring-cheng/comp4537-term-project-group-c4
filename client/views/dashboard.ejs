<%- include('partials/header', { title: "User Dashboard" }) %>

    <div class="admin-container">
        <h1 class="page-title" id="dashboard-title">User Dashboard</h1>

        <section class="stats-section">
            <h2 id="your-information">Your Information</h2>
            <div class="stats-grid">
                <div class="stat-card">
                    <h3 id="email-label">Email</h3>
                    <p id="user-email">Loading...</p>
                </div>
                <div class="stat-card">
                    <h3 id="api-calls-label">Total API Calls</h3>
                    <p id="total-api-calls">0</p>
                </div>
            </div>
        </section>

        <button id="testAiBtn" onclick="window.location.href='/ai-test'"
            style="background: #0074d9; margin-top: 1rem;">Test AI Model</button>

        <button id="logoutBtn" class="logout-btn">Logout</button>

        <!-- Delete Account Button (hidden for admin) -->
        <button id="deleteAccountBtn" class="delete-account-btn"
            style="background: #dc3545; margin-top: 10rem; display: none;">
            Delete Account
        </button>
    </div>

    <script type="module" src="dashboard.js"></script>

    <%- include('partials/footer') %>